letters = ['a', 'b', 'c', 'd', 'a', 'a', 'g']

print(letters.count('a'))
if 'b' in letters:
    print('b is in the letters list')

# We get an error if we  try to print a leetr which is not in the list : Value Error
