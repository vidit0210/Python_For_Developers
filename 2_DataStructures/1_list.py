l = [1, 2, 3, 4]
matrix = [[0, 1], [2, 3], [4, 5]]


# Assume want to make a list of more than 100 items of value 0
zeros = [0]*100
print(len(zeros))

# Joining 2 list together  concatenation
combined = l + matrix

print(combined)

numbers =  list(range(20))#Any interable can be passes in the list

name = list('Vidit')
print(numbers)
print(name)